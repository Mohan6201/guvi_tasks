# DEPRECATED - kept only for reference.
#
# A static base64 secret here goes stale (ECR tokens expire after 12h),
# so the ECR pull secret is now created imperatively instead, driven by
# .env, every time deploy.sh / buildspec-deploy.yml runs:
#
#   kubectl create secret docker-registry ecr-registry-secret \
#     --docker-server=${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com \
#     --docker-username=AWS \
#     --docker-password=$(aws ecr get-login-password --region ${AWS_REGION}) \
#     --namespace=${K8S_NAMESPACE} \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# See k8s/deploy.sh and buildspec-deploy.yml.
